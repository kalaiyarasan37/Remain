const Config = {
  API_BASE_URL: 'https://your-api.com/api', // replace later
  OTP_EXPIRY_SECONDS: 60,
  APP_NAME: 'RemainApp',
  MAX_REMINDERS: 100,
  VOICE_LOCALE: 'en-IN',
};

export default Config;